import React, {useContext} from 'react';

export default function UseContent(props) {
    return (
        <div>
            {
                console.log(props)
            }
        </div>
    )
}

